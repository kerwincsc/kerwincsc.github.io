go 安装
======================================================================

yum 安装 go 之后, 需要在 bashrc 中配置如下变量:

.. code-block:: shell

   export GOPATH=/path/to/go
   export PATH=/path/to/go/bin:$PATH
